from setuptools import setup, find_packages
from codecs import open
from os import path

here = path.abspath(path.dirname(__file__))

setup(
    name='ppo',
    version='1.0',
    description='PPO Repo MiME',
    packages=find_packages(),
)
